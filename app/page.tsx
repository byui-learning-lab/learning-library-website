export default function Home() {
  return (
    <div className="mx-auto max-w-3xl px-6 py-24 text-center">
      <h1 className="font-heading text-4xl font-semibold text-primary-dark">
        Learning Library
      </h1>
      <p className="mt-4 text-neutral">
        Home page content (hero, audience entry points, recent-updates feed)
        comes together in Phase 4 — this is just the Phase 3 layout shell
        wrapping it.
      </p>
    </div>
  );
}
